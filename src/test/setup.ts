/**
 * Vitest global test setup.
 *
 * This file runs before every test suite. Add global mocks,
 * polyfills, or test environment configuration here.
 */

// Intentionally minimal — add setup as needed.
