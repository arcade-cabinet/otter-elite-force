/** Game viewport dimensions — shared by scenes and config without circular imports. */
export const GAME_WIDTH = 1280;
export const GAME_HEIGHT = 720;

/** Tile size in pixels — shared by all systems that convert between tile and pixel space. */
export const TILE_SIZE = 32;
